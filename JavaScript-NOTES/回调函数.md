# 回调函数

如果将函数A做为参数传递给函数B时，函数A为回调函数

当一个函数当作参数来传递给另一个函数时，这个函数就是回调函数

常见的使用场景：

```
function fn() {
  console.log('我是回调函数...')
}

// fn传递给了setInterval，fn就是回调函数
setInterval(fn, 1000)
```

